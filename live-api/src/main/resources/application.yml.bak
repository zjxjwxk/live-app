spring:
  application:
    name: live-api

dubbo:
  application:
    name: ${spring.application.name}
    qos-enable: false
  registry:
    address: nacos://live.zjxjwxk.com:8848?username=nacos&&password=nacos

server:
  servlet:
    context-path: /live/api